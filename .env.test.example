# Test env template for Playwright /app + /waitlist suites

# Base URL used by Playwright tests
BASE_URL=http://127.0.0.1:3000
WAITLIST_PATH=/waitlist/index.html

# App gate (must match the server env used by vercel dev)
APP_ACCESS_KEY=test-app-access-key
E2E_APP_BEACH_ID=BR-RN-001
E2E_APP_UI_PATH=/app/

# Optional: deterministic API behavior for local/CI
WAITLIST_TEST_MODE=1
REPORTS_TEST_MODE=1
TEST_RATE_LIMIT=10
ANALYTICS_RATE_LIMIT=120

# Supabase (required for authenticated favorites/login tests)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: authenticated E2E user for tests/app.favorites.spec.ts
E2E_TEST_USER_EMAIL=
E2E_TEST_USER_PASSWORD=

# Optional analytics hardening
ANALYTICS_SALT=
